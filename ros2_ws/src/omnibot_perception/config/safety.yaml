simple_safety_stop_node:
  ros__parameters:
    danger_distance: 0.1      # опасная дистанция (метры)
    warning_distance: 0.3     # дистанция предупреждения (метры)
    safety_angle: 30.0        # угол обзора (градусы)
    enable_logging: true      # включить логирование